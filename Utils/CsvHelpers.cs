namespace DataWhisperIngest.Utils;

public static class CsvHelpers
{
    public static string[] SplitCsvLine(string line) =>
        line.Split(','); // placeholder for smarter CSV parsing if needed later
}
